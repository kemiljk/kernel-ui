---
// Wraps a set of <Example> cards in a two-column grid — the "proper
// examples, nicely laid out" section that sits between a component's
// quick-glance demo and its interactive Playground. See Example.astro
// for what an individual card looks like.
---

<div class="example-grid">
  <slot />
</div>
