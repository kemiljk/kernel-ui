---
"@kernelui-lib/react": minor
"@kernelui-lib/elements": minor
---

Add `presentation="disclosure"` to DropdownMenu for menus anchored within their parent, including fixed mobile navigation. A native details/summary owns toggling and CSS owns repeated entry/exit, without a top-layer backdrop, scroll locking, or JavaScript animation timers. Both packages retain keyboard roving, disabled items, Escape/outside dismissal, selection and reduced-motion behavior. Render a summary trigger; the default popover presentation is unchanged.
